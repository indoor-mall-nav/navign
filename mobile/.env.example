# Mobile App Environment Configuration

# API Base URL - for server API endpoints (navigation, etc.)
# Only needed when running as standalone web app (not in Tauri mode)
# When running in Tauri, the app will use Tauri commands instead of HTTP requests
VITE_API_BASE_URL=http://localhost:3000

# Orchestrator URL - for admin CRUD operations
# The orchestrator is the only service allowed to modify the central database
# Default: http://localhost:8081
VITE_ORCHESTRATOR_URL=http://localhost:8081

# For production deployment (standalone web app)
# VITE_API_BASE_URL=https://api.yourserver.com
# VITE_ORCHESTRATOR_URL=https://orchestrator.yourserver.com
