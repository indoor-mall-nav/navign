//! PostgreSQL models - re-exported from navign-shared
//!
//! All PostgreSQL models are defined in the shared crate with the `postgres` feature.
//! This avoids duplication and keeps schema definitions centralized.

pub use navign_shared::schema::postgres::*;
