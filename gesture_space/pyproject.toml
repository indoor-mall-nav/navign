[project]
dependencies = [
  "openai>=2.6.1",
  "requests>=2.31.0",
  "torch>=2.9.0",
  "transformers>=4.40.0",
]
description = "Add your description here"
name = "gesture-space"
readme = "README.md"
requires-python = ">=3.12"
version = "0.1.0"
