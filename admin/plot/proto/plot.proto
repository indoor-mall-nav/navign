syntax = "proto3";

package plot;

message Image {
    bytes data = 1;
    string format = 2; // e.g., "png", "jpeg"
    int32 width = 3;
    int32 height = 4;
    string label = 5;
}

message PlotRequest {
    string plot_id = 1;
    repeated Image images = 2;
    string description = 3;
}
